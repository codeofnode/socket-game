{
  "name": "{{pkg.name}}-client",
  "version": "{{pkg.version}}",
  "description": "socket client for socket-game",
  "dependencies": {
    "socket.io-client": "^2.0.3",
    "babel-runtime": "^6.23.0",
    "json2server": "^0.1.18",
    "util.promisify": "^1.0.0"
  }
}
